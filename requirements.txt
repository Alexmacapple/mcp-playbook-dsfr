# MCP DSFR Server Dependencies
# 
# Installation production : pip install -r requirements.txt
# Installation développement : pip install -r requirements-dev.txt

# ========== REQUIRED (7 packages) ==========
# Ces dépendances sont OBLIGATOIRES pour faire fonctionner le serveur

mcp>=0.1.0                    # Model Context Protocol SDK
beautifulsoup4>=4.12.0        # HTML parsing pour l'audit RGAA
lxml>=4.9.0                   # Parser XML/HTML pour BeautifulSoup
bleach>=6.0.0                 # Sanitization HTML sécurisée
typing-extensions>=4.0.0      # Compatibilité types Python 3.8+
python-dotenv>=1.0.0          # Variables d'environnement
requests>=2.31.0              # Client HTTP (outil check_dsfr_version)